# Academic Conference Assistant Agent

这是一个基于 AI 的 **学术会议参会助手**，帮助用户从多个维度高效规划并参与学术会议，丰富参会体验，结交新的朋友。

## 🚀 功能概述

此系统包含以下几个模块，旨在为用户提供完整的参会辅助服务：

1. **Part 1: 论文与作者信息抓取与邮件生成**
    - 根据用户的研究方向，筛选相关会议论文，提取相关一作作者，并自动生成邮件，方便用户联系学者进行 Coffee Chat 或其他交流。
   
2. **Part 2: 会议日程抓取与个性化推荐**
    - 自动抓取学术会议官网的日程数据，生成用户感兴趣的个性化参会日程，并生成对应的日程海报。

3. **Part 3: 会场与城市指南**
    - 根据用户指定的会议和年份，查询会议的举办城市与场馆，并推荐附近餐厅与旅游景点，方便用户的参会和旅行安排。

4. **Part 4: 小红书参会攻略生成**
    - 根据前面模块生成的所有信息，自动生成一个 **小红书参会攻略**，包括文字和配图，帮助用户将参会信息分享给社交网络。

## 📦 系统架构

```text
Academic Conference Assistant Agent
├── agent_part1/
│   ├── agent_part1_contacts.py    # 论文筛选与邮件生成
│   ├── coffee_chat_email.json   # 保存作者信息及邮件内容
├── agent_part2/
│   ├── fetch_schedule.py         # 抓取会议官网日程
│   ├── personalize_schedule.py   # 生成个性化日程
│   ├── render_schedule_image.py  # 生成日程海报
│   ├── schedule2prompt.py        # 生成海报的prompt
│   ├── your_personalized_schedule.json # 保存定制日程
├── agent_part3/
│   ├── part3_agent.py           # 获取会场与城市指南
│   ├── fetch_conference_venue.py
│   ├── recommend_places.py
│   └── city_tour_guide.json  # 保存城市与餐厅景点信息
├── agent_part4/
│   ├── part4_agent.py           # 小红书参会攻略生成
│   ├── generate_xhs_images.py
│   └── generate_xhs_text.py
├── outputs/
│       ├── xhs_post.txt         # 小红书文案
│       └── xhs_images/          # 生成的配图
├── requirements.txt             # 项目依赖包
├── README.md                    # 项目说明文档
└── run.sh                       # 一键运行生成coffee chat邮件，小红书图文等
```
---
## 🔧 安装依赖

在运行该项目之前，确保安装了 Python 环境，并安装以下依赖：
``` bash
pip install -r requirements.txt
```
---

## 🚀 一键运行（推荐）：使用 run.sh

为了避免用户在 Part1–Part4 中重复输入相同参数（会议名、年份、研究方向等），本项目提供了统一的 `run.sh` 脚本。

用户 **只需在 run.sh 顶部配置一次参数**，即可一键完成：

- Part1：作者筛选 + Coffee Chat 邮件
- Part2：日程抓取 + 个性化日程 + 日程海报
- Part3：会场城市 + 餐厅 & 景点推荐
- Part4：小红书参会攻略（文 + 四宫格配图）

---

### 1️⃣ 修改 run.sh 中的统一配置

打开 `run.sh`，只需要修改以下变量：

```bash
# =========================
# User Config (EDIT HERE)
# =========================
CONFERENCE="CVPR"
YEAR=2025
CITY="Nashville"

USER_NAME="Jason"
USER_RESEARCH="Self-supervised learning for medical image analysis"

KEYWORDS="registration self-supervised 3d"

FOCUS_SUMMARY="Focus on self-supervised learning and medical imaging sessions."
```
### 2️⃣ 一键运行整个 Agent
``` bash
sh run.sh
```
### 3️⃣ 运行完成后的输出内容

运行成功后，你将获得：

| 模块  | 输出内容                                                                 |
|-------|--------------------------------------------------------------------------|
| Part1 | agent_part1/coffee_chat_email.json（作者 + 邮件）                       |
| Part2 | agent_part2/schedule_data/（原始日程）<br>*your_personalized_schedule.json（个性化日程）<br>daily_schedules/*.png（日程海报） |
| Part3 | agent_part3/city_tour_guide.json（城市指南）                        |
| Part4 | outputs/xhs_post.txt（小红书文案）<br>outputs/xhs_images/*.png（四宫格配图） |
---
### 🎉 至此，你可以直接发布：

- Coffee Chat 邮件（Part1）

- 会议参会日程图（Part2）

- 小红书参会攻略（Part4）

## 📋 Step by Step 使用说明
### Part 1: 论文与作者信息抓取与邮件生成
#### 1️⃣ 运行论文筛选与邮件生成模块
``` bash
python agent_part1/agent_part1_contacts.py \
  --conference CVPR \
  --keywords "registration" "self-supervised" "3d" \
  --user_name "Siyan" \
  --user_research "self-supervised 3D registration and medical image analysis"
```

- 功能: 该模块会根据指定的会议（如 CVPR）、研究方向关键词（如 registration, self-supervised, 3d），从论文数据库中筛选相关论文，提取相关作者信息，并为用户生成 Coffee Chat 邮件。

- 输出: 生成的 JSON 文件（coffee_chat_email.json）包含作者信息及邮件内容。
---

### Part 2: 会议日程抓取与个性化推荐
#### 1️⃣抓取会议官网日程
``` bash
python agent_part2/fetch_schedule.py --conference CVPR --year 2025
```

- 功能: 抓取并保存指定会议（如 CVPR 2025）的官网日程。将保存原始 HTML 文件和清洗后的文本文件。

- 输出: 会议信息保存为 schedule_data/ 目录下的 .html 和 .txt 文件。

#### 2️⃣ 生成个性化参会日程
```bash
python agent_part2/personalize_schedule.py --conference CVPR --year 2025 --keywords "registration" "self-supervised" "3d"
```

- 功能: 基于抓取到的会议日程文件，按照用户提供的关键词生成个性化参会日程，推荐最符合用户研究方向的会议内容。

- 输出: 生成的个性化日程为 JSON 格式，并保存为文件。

#### 3️⃣ 生成日程海报
```bash
python agent_part2/render_schedule_image.py --conference CVPR --year 2025 --city Nashville
```

- 功能: 基于用户的个性化日程，生成包含日程信息的海报图片。

- 输出: 生成的海报图片将保存到 agent_part2/daily_schedules 目录中。
---
### Part 3: 会场与城市指南
#### 1️⃣ 获取会议场馆与城市信息
``` bash
python agent_part3/part3_agent.py --conference CVPR --year 2025 --city Nashville
```

- 功能: 根据指定的会议（如 CVPR 2025）和城市（如 Nashville），查询该会议的场馆信息，并推荐附近的餐厅与旅游景点。

- 输出: 生成的城市与场馆信息保存在 city_tour_guide.json。
---
Part 4: 小红书参会攻略生成
6️⃣ 生成小红书参会攻略（文 + 图）
```bash
python agent_part4/part4_agent.py \
  --user_name "Siyan" \
  --user_research "Self-supervised learning for medical image analysis" \
  --conference CVPR \
  --year 2025 \
  --your_focus "Focus on self-supervised learning and medical imaging sessions." 
```

- 功能: 该模块将从 Part1, Part2, Part3 中提取数据，自动生成一篇小红书（XHS）参会攻略的文案，并生成与之匹配的配图（四宫格模式）。

- 输出: 生成的文案保存在 outputs/xhs_post.txt，生成的配图保存在 outputs/xhs_images/ 目录。
---
## ⚙️ 依赖项

请确保以下 Python 库已安装：

- requests - 用于 HTTP 请求

- beautifulsoup4 - 用于解析 HTML

- openai - 用于与 Qwen API 进行交互（需要配置 API key）

- dashscope - 用于生成图像（需要配置 API key）

使用以下命令安装所有依赖：
```bash
pip install -r requirements.txt
```
---
## 🔑 配置 Qwen API Key

若项目中涉及到与 Qwen API 的交互（例如生成邮件、日程海报等），请按照以下步骤配置你的 API Key：

1. 创建 .env 文件并添加你的 API Key：
``` bash
DASHSCOPE_API_KEY="your_api_key_here"
```

2. 确保你的系统能够读取环境变量，或者在代码中手动指定 API Key。
---
## 🧑‍💻 作者与许可

* 作者：[Siyan Zhuo](https://github.com/Zhuosy)
* License: **Unlicense**（公共领域，无任何使用限制）

> 你可以自由地使用、修改、分发本项目的全部或部分内容，无需署名或许可。